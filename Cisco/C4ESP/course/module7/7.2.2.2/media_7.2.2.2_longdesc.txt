Figure one shows the output from the show i.p. interface brief pipe include tunnel command and the show interface tunnel zero command on router one. Figure two shows the output from the show i.p. o.s.p.f. neighbor command on router one. Figure three is an interactive activity that allows the learner to practice configuring a G.R.E. tunnel between two routers.