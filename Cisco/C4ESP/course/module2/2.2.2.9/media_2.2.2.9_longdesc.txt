Figure one is an interactive activity that allows the learner to match WAN access terms with their corresponding description. Figure two is an interactive activity that allows the learner to match WAN access terms with their corresponding description. Figure three is an interactive activity that allows the learner to match a WAN access type with its corresponding private WAN access option.