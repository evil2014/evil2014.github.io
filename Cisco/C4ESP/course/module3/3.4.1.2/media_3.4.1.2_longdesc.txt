Figure one shows the output from the command debug p.p.p. packet.  This output shows al of the p.p.p. packets being sent and received by the router. Figure two shows the output from the command debug p.p.p. negotiation.  This output displays all of the p.p.p. negotiation transactions. Figure three shows the output from the command debug p.p.p. error.  This output shows protocol errors and error statistics.