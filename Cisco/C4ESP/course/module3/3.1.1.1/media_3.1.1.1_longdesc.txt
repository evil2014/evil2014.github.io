Figure one shows two routers. Each router is connected to a WAN provided by an Internet Service Provider.  Each router has a LAN on an Ethernet interface. Figure two is an animation comparing a serial connection to a parallel connection.  The serial connection sends one bit, while the parallel connection is sending eight bits.  