Figure shows a router sending a frame to a remote router through a WAN cloud.  The sending router begins to transmit binary bits zero one one one one one zero zero, and the receiving router begins to receive the bits in the order in which they were sent.  