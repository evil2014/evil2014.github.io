Figure shows two routers connected using two serial ports from each router and forming a p.p.p. multilink bundle.  The figure shows the output from the configuration file, and is highlighting the lines that refer to the bundle.  The first highlighted section creates the bundle and the first line is p.p.p multilink.  The second line is p.p.p. multilink group 1.  Each of the two serial interfaces used in the multilink bundle have the same two lines as in the configuration file.