Figure one shows router two with an inside network on the left and an outside network on the right. The inside network contains router one and two computers. The outside network contains a server.  Below is a nat table showing the translations. Figure two shows a table listing the steps and commands involved in configuring pat. Figure three is an interactive activity that allows the learner to practice configuring pat.