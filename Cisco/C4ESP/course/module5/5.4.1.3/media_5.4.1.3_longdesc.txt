Figure shows router one with two lans connected. Router one is also connected to router two via a serial link.  Router two is also connected to an i. s. p.  The networks connected to router one are internal and they use private addressing.  There is a callout stating that this is a stub network.  Router two has a callout stating that it is a NAT enabled border router.