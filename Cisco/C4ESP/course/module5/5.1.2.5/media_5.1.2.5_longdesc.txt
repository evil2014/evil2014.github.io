Figure shows two tables.  One table shows how nat uses a pool of global addresses from which to choose. The second table shows how pat uses a single address, but adds a unique port number to the mapping.