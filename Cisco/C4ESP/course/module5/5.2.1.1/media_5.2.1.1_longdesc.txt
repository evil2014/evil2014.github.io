Figure one shows router two with an inside network with a web server on the left and an outside network with a p. c. on the right.  A static nat table is listed below the image. Figure two shows a table listing the commands and descriptions for configuring static nat. Figure three shows the syntax for configuring static nat. Figure four is an interactive activity that allows the learner to practice configuring static nat.