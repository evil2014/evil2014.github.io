Figure one shows the output from the "debug i. p. nat" command. Figure two shows router 2 with an inside network on the left and an outside network on the right. The inside network contains 2 computers and router one. The outside network contains two servers.  Router two is using pat.   The output from the "show access dash lists" command is also shown.