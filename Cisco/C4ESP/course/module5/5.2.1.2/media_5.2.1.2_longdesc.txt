Figure shows router two with an inside network with a web server on the left and an outside network with a p. c. on the right.  There are packets going back and forth between the p. c. and the web server.  There are numbers next to the items indicating the order in the translation process for static nat. A static nat table is listed below the image.  