Figure shows the commands used to configure point to point sub interfaces.  The command is run in interface configuration mode and the command is interface serial.  This is followed by the current interface number, followed by a sub interface number between 1 and 4,294,967,293, and then followed by either the multipoint or point hyphen to hyphen point designation.