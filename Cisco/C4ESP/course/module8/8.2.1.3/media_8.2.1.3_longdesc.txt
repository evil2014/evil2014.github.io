Figure one shows a worker sending an s.n.m.p. get to check the m.i.b. variable on gig zero slash zero on router one.  Router one gig zero slash zero is down, so router one sends an s.n.m.p. trap to the manager.  The manager can be configured to send a text alert to the network administrator. Figure two shows the exchange of all s.n.m.p. messages.