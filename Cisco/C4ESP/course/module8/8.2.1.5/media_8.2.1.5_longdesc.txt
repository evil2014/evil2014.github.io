Figure is an animation.  The animation includes a worker on a P.C. as the s.n.m.p. management station, a web server with s.n.m.p. agent, and a central m.i.b. database.  The manager requests statistics from the web server and finds out there are ten thousand users.