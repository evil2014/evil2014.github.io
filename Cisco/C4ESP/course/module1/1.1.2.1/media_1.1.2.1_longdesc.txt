Figure one is an animation.  A single flat network design is broken into smaller segments that are interconnected utilizing the three layer switching design. Figure two is an example of a multi building enterprise network.  There are several buildings, and they all connect to a single building.