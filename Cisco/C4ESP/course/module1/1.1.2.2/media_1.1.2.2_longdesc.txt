Figure shows the hierarchical design model.  A business has two connections to the Internet, and below those connections is an example of the three layer design, the core switches, distribution layer switches, and access layer switches.  The access layer is highlighted.