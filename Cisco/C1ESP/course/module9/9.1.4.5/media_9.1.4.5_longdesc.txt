The figure is an interactive activity that allows the learner to practice calculating the subnet mask in binary, subnet mask in dotted decimal, and the prefix notation for several different host requirements.