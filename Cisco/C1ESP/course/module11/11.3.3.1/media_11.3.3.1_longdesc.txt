Figure one shows the output from a "show running dash config" command on a router. Figure two shows the output from a "show interfaces" command on a router. Figure three shows the output from a "show arp" command on a router. Figure four shows the output from a "show i.p. route" command on a router. Figure five shows the output from a "show protocols" command on a router. Figure six shows the output from a "show version" command on a router.