The figure shows the Internet layer handing the packets to the Network Access layer.  The network Access layer encapsulates the packets into frames and converts the frames into bits (ones and zeros) to be placed on the wire.