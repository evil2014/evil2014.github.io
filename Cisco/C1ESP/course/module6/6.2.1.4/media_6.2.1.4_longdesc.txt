Figure shows the I.P. version 4 routing table on a P.C. with the network destinations grouped and highlighted with different colors.  The default gateway is highlighted in yellow.  The loopback addresses are highlighted in  green.  The LAN addresses (host address, LAN network I.D. and LAN broadcast address) are highlighted in purple.  The multicast addresses are highlighted in pink and the broadcast addresses are highlighted in orange.