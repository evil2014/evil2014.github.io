Figure shows a network with two routers connected by a serial link.  Each router has 2 LANs.  There is a callout showing that router 1 has 3 directly connected networks and 2 remote networks in its routing table.