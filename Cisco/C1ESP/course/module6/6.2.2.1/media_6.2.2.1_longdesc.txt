Figure shows two routers connected by a serial connection.  Each router has two LANs attached.  This figure shows that a router has its own routing table to consult should a packet be destined for a remote network.  The routing table consists of directly connected networks and remote networks.