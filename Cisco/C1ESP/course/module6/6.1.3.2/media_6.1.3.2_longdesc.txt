Figure shows a sample I.P. header with several fields highlighted.  These include header length, total length, identification,  flags, fragment offset and the header checksum.  These fields are used for validation and reordering the packets at the destination.