Figure shows a router connected to a LAN with two hosts and the Internet with a remote server.  There is an arrow from one host to another host on the same network.  There is also an arrow from one host to the remote server.