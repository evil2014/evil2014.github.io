Figure shows a router connected to a LAN with two hosts and the Internet with a remote server.  There is a note pointing to the router showing that the I.P. address of the server's LAN interface is the default gateway address for the hosts on that LAN.