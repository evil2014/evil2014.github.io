Figure one shows a Transport layer P.D.U., the segment, as the segment header and the data from the upper layers. Figure two shows how layer 3, the network layer, encapsulates the transport layer P.D.U. into a packet and adds the I.P. Header.