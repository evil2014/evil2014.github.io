Figure lists the four layers of the T.C.P.I.P. model and a brief description of what each layer provides.  The layers from top down are as follows:  The Application layer represents data to the user, plus encoding and dialog control.  Transport layer supports communication between diverse devices across diverse networks.  The Internetwork layer determines the best path through the network.  The Network Interface layer controls the hardware devices and media that make up the network.