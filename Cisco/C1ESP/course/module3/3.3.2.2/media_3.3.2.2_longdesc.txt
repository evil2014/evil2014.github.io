Figure shows the source and destination MAC addresses being added to an Ethernet frame.  PC 1 is the source and has a logical address of A A dash A A dash A A dash A A dash A A dash A A.  The server is the destination and it has a source address of C C dash C C dash C C dash C C dash C C dash C C.  These two addresses are placed into the frame header.