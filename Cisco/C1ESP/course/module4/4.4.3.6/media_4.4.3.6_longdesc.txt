This animation shows how ring topologies work.  When Host A wishes to send to Host D, the packet is placed on the line and travels around  the ring.  As it passes each host, the host checks to see if it is for that host.  If not, it ignores it.