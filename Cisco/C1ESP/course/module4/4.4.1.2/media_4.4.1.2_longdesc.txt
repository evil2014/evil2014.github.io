Figure one shows a floor plan with the location of each network drop and where it is on the floor plan.  This is called a physical topology. Figure two shows a router that has two interfaces each connected to a LAN.  Each lan has some rooms with computers and I.P. addresses.  This is called a logical topology.