Figure one is an interactive activity that allows the learner to match the field name with its corresponding position and size for an Ethernet frame. Figure two is an interactive activity that allows the learner to match the field name with its corresponding position and size for a p.p.p. frame. Figure three is an interactive activity that allows the learner to match the field name with its corresponding position and size for a wireless frame.