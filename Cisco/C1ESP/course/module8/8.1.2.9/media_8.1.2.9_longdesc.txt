The figure is an interactive media element that presents the learner with a host address and a subnet mask in both decimal and binary.  The learner must perform the anding and provide the network address in both binary and decimal.