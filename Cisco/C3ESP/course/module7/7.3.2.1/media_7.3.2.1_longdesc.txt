Figure one shows the mathematical calculation of the e. i. g. r. p. metric.  The formula shows that when the k. values are at their default, parts of the equation are ignored and the default calculation is simply bandwidth plus delay.  The default values for k. 1 and k. 3 are one. The default values for k. 2, k. 4, and k. 5 are zero.  Below is the syntax for configuring the k. values under the e. i. g. r. p. routing configuration. Figure two shows the output of the "show i. p. protocols" command, highlighting the k. values.