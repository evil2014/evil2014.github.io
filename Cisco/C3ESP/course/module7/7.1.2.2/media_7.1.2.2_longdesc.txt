Figure shows three interconnected routers.  Each has a hello packet between them.  There is a table listing the hello timers for different link speeds.  A T 1, or slower, sends hello packets every sixty seconds.  A link faster than a T 1 sends hello packets every five seconds.