Figure shows three interconnected routers.  Each router has an image of a lock on it, and there is a bidirectional arrow between each router.  This demonstrates that e. i. g. r. p. can use authentication before sharing routing information.