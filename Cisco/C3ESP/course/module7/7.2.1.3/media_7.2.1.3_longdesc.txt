Figure one shows the standard lab diagram with the extra link between router one and router three.  Router two is connected to i. s. p. and the internal network is highlighted excluding the link between router two and i. s. p. Figure two shows the output of conf t. on router one, and then the output for router question mark.  The output lists the different routing protocols available. Figure three shows the command "router e. i. g. r. p. one" on each of the routers.