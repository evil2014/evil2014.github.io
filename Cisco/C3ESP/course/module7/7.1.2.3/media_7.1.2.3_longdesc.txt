Figure shows three interconnected routers.  The router two lan is down.  There is an update packet traveling from router two to routers one and three.  Routers one and three are sending an acknowledgement packet back to router two.