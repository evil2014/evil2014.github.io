Figure shows three interconnected routers.  The router two lan is down.  There is a query packet traveling from router two to routers one and three.  Routers one and three are sending a reply packet back to router two.