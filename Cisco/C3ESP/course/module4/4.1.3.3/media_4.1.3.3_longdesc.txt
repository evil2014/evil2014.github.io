Figure one shows two scenarios, one with a single laptop connected to a wireless access point and another with two laptops connected to a wireless access point.  Both are considered a basic service area.  Below the image is a table listing the basic service set summary information. Figure two shows two scenarios, one with a single laptop connected to a wireless access point and another with two laptops connected to a wireless access point.  Both are considered a basic service area. Both of these areas are connected to the same network infrastructure.  This entire area is considered an extended service area.  Below the image is a table listing the extended service set information.