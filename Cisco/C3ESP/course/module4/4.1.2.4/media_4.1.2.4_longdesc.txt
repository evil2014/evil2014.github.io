Figure one shows a router, connected to a switch, which is connected to another switch with two p.c's connected.  The second switch has an autonomous access point connected.  There are two laptops connected wirelessly to the access point. Figure two shows a router, connected to a switch, which is connected to another switch with two p.c's connected.  The second switch has a controller-based access point connected along with the wireless lan controller.  There are two laptops connected wirelessly to the access point.