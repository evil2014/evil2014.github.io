Figure shows the bridge i.d. fields in two formats.  One with extended system i.d. and one without.  Without, the priority takes 2 bytes.  With, the priority uses 4 bits and the system i.d. uses the other 12 bits.