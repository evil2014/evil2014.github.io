Figure one shows three switches inter-connected with trunk links.  Switch one is the root bridge. Figure two shows a table that lists commands for configuring rapid p.v.s.t. plus and a description. Figure three shows the commands for configuring switch one to use rapid p.v.s.t. and set interface fast ethernet zero slash two as a point to point link type. Figure four shows the output from the show spanning dash tree v. lan ten command. Figure five shows the output from the show run command.