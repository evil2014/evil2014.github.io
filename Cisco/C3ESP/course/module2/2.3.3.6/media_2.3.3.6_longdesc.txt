Figure one is an interactive activity that allows the learner to select the root bridge based on the output from the show spanning dash tree v. lan one command on switch one. Figure two is an interactive activity that allows th learner to select the switch and port that are in blocking mode based on the output from the show c.d.p. neighbors command on switch three. Figure three is an interactive activity that allows the learner to identify the link that is down based on the output from the show spanning dash tree command on switch two.