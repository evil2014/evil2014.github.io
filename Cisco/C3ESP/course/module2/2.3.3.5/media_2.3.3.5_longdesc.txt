Figure shows four switches that are inter-connected.  Each port is labeled with its mode, and switch two is labeled as the root bridge.  The loop has been eliminated, and spanning tree is once again functioning as expected.