Figure shows four switches that are inter-connected.  Each port is labeled with its mode, and switch two is labeled as the root bridge.  The output from the show spanning dash tree v. lan one hundred is also displayed.