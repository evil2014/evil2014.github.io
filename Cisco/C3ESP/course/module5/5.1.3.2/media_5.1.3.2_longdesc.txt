Figure one shows the output from the "show i. p. route" command.  The gateway of last resort and the default status route are highlighted. Figure two is an interactive activity that allows the learner to practice listing the routing table for router two and router three in order to verify that the default static route is configured and propagates to router three correctly.