Figure one shows the standard lab diagram with the extra link between router one and router three.  Router two is connected to i. s. p. The internal network is highlighted, excluding the link between router two and i. s. p. Figure two shows the output from the "show i. p. route" command on router one. Figure three shows the output from the "show i. p. protocols" command on router three.  The networks for which router three is routing are highlighted. Figure four shows the output from the "show running dash config pipe section router o. s. p. f." command on router three. Figure five shows the syntax for configuring a network for o. s. p. f. Figure six shows the output from the "show i. p. route o. s. p. f." command on router one.