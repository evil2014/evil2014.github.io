Figure one shows the output from the show i.p. protocols command on a router. Figure two shows the output from the show i.p. route pipe begin gateway command on a router. Figure three shows the output from the show i.p. o.s.p.f. neighbor command on a router. Figure four shows the output from the show interfaces gigabit ethernet zero slash zero command on a router. Figure five shows the output from the show i.p. interface gigabit ethernet zero slash zero command on a router. Figure six shows the output from the show i.p. interface brief command on a router. Figure seven shows the output from the show protocols command on a router. Figure eight shows the output from the show c.d.p. neighbors command on a router. Figure nine is an interactive activity that allows the learner to practice using the commands discussed in this section.