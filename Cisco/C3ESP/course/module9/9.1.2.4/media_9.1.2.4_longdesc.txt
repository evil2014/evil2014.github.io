Figure one shows the syntax for setting the router to boot from a specific i. o. s. file in flash.  This is helpful when you are testing a new version and do not want to remove the old one yet. Figure two shows the output from the "show version" command on router one.  The new Cisco i. o. s. version and filename are highlighted.