Figure shows a network where router one is connected to i. s. p. and router two.  Router two summarized the one seventy two dot sixteen dot zero dot zero and sent it to router one.  Router two also has a default static route pointing to router one.  In this case, a null zero interface is used for the prevention of a routing loop.