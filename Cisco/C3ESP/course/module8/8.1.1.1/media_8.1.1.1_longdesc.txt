Figure one shows the standard lab diagram with the extra link between router one and router three.  Router two is connected to i. s. p. and the internal network is highlighted excluding the link between router two and i. s. p.. Figure two is the output from the "show running dash config" command on router one.  The version of the I.O.S. and the bandwidth for serial zero zero zero are highlighted. Figure three is the output from the "show running dash config" command on router two.  The version of the I.O.S. and the bandwidth for serial zero slash zero slash zero and serial zero slash zero slash one are highlighted. Figure four is the output from the "show running dash config" command on router three.  The version of the I.O.S. and the bandwidth for serial zero zero zero are highlighted.