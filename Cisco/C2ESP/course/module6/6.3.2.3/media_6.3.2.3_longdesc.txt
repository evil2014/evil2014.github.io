Figure one shows three routers connected to a single router that is connected to router one.  The diagram shows the syntax to configure static routes on router one to all of the networks.  Figure two shows three routers connected to a single router that is connected to router one.  The diagram shows the syntax for removing the static routes on router one and replace it with a summary route.