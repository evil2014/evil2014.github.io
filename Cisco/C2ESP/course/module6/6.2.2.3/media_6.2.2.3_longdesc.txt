Figure shows the standard lab diagram with the output of the "show ip route static" command.  This shows the relationship between the Gateway of last resort at the top of the output and the default static route at the bottom.