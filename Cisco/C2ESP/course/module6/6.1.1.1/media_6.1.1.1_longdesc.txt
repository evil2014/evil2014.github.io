Figure one shows a network with 2 connected routers.  Each router has two lans and one router also connects to the Internet.  This example shows that we can use static routes to point to the lans on the other router and to the Internet. Figure two shows a network with 2 connected routers.  Each router has two lans and one router also connects to the Internet. This example uses E. I. G. R. P. to share routes with the neighboring router.