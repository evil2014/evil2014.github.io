These figures are based on the standard lab diagram.  R. 1. with a lan connects to R. 2. with a lan which connects to R. 3. with a lan. Figure one shows the output of the command "show i. p. route pipe begin gateway" on router one. Figure two shows the output of the command "show i. p. route one ninety two dot one sixty eight dot two dot one" on router one. Figure three shows the output of the command "show running dash config pipe section i. p. route". Figure four is an interactive activity that allows the learner to practice verifying static routing settings on router two. Figure four is an interactive activity that allows the learner to practice verifying static routing settings on router three.