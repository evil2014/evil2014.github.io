Figure shows two routers, each with a lan, connected to a single router which is connected to router one.  Router one has a callout that shows it needs to create a summary route to reach the remote networks.