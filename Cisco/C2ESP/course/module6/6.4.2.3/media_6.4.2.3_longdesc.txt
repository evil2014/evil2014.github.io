These figures show two routers, each with a lan, connected to a single router which is connected to router one. Figure one shows the syntax for removing the previously configured static routes and configuring a summary route on router one to remote networks using i. p. v. 6.Figure two shows the output of the "show i. p. v. 6 route static" command on route one.