Figure one shows the output from the command, show i.p. route pipe begin Gateway. The three directly connected routes, the five routes learned through RIP, and the static default route are highlighted.  This is showing all of the ultimate routes because each of the destination network entries has a net hop address or an exit interface.