Figure shows router R-1 with an active Fast Ethernet interface and connected to three other routers over each of three WAN links.  Figure is an animation that shows the router sending Hello packets out all four active interfaces.  The three connected routers return Hello packets to router R-1.