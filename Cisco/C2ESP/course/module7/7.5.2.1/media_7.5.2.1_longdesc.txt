Figure shows the output from the command show i.p. route pipe begin Gateway.  This shows the routing table for router R-1, which has three directly connected routes with three interface I.P. addresses, five routes learned through RIP, one candidate default route, and one gateway of last resort configured.