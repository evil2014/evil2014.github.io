Figure shows the contents of the link state database on router R-1.  This table lists the link states for all of the routers in the local network.  For example, the database lists all of router R-1’s connected networks, the cost of each link, and the link states for routers R-2, R-3, R-4, and R-5.