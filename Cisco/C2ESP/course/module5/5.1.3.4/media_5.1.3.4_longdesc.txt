Figure one shows the output of the command, show v.lans, from the perspective of a router that has been configured for router-on-a-stick inter-v.lan routing. Figure two shows the output of the ip routing table from the perspective of a router that has been configured for router-on-a-stick inter-v.lan routing. Figure three is a syntax checker that demonstrates the process of creating sub-interfaces, setting the encapsulation, and activating the physical interfaces on a router which is being used for router-on-a-stick inter-v.lan routing.