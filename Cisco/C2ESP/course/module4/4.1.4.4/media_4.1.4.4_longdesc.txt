Figure one shows the command terminal history size 200 being entered on a router thereby setting the history to remember the 200 most recent commands.  The command show history is then run on the router and the output lists the most recent commands entered on the router. Figure two is an interactive media element that allows the learner to practice with the terminal history size and show history commands.