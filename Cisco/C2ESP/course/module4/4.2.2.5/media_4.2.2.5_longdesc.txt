Figure one is an interactive media element that presents a packet forwarding scenario and allows the learner to determine the action that the router will take. Figure two is an interactive media element that presents a packet forwarding scenario and allows the learner to determine the action the router will take. Figure three is an interactive media element that presents a packet forwarding scenario and allows the learner to determine the action the router will take. Figure four is an interactive media element that presents a packet forwarding scenario and allows the learner to determine the action the router will take.