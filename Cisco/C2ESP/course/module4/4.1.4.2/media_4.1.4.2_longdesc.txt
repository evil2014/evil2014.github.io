Figure one shows the output of the show i.p. v 6 interfaces brief command.  This lists address and status only information for the interfaces. Figure two shows the output of the show i.p.v.6. interface gigabit ethernet zero slash zero command.  This lists more detailed information, such as the multicast address assigned to the interface. Figure three shows the output from the i.p.v.6. route command, which is the i.p.v.6. route table for the router. Figure four shows usage and output of the ping command on an i.p.v.6. interface.  The command is ping 2 0 0 1 colon d b 8 colon a c a d colon 1 colon colon 10.  