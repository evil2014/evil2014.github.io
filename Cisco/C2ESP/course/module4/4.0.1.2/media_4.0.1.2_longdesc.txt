Figure shows two LANS. Each LAN has a router. The LANs are connected to each other by a WAN link.  The figure displays the output of the show i.p. route command on one of the routers.  The result of this command will list all networks known to the router and the paths to reach those networks.  However, on this router, routing has not been configured and the routing table is empty.