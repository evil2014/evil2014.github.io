Figure shows two routers that are connected through a WAN link and both are running the dynamic routing protocol E.I.G.R.P.  Each router has three connected networks and they advertise those networks to the other router.