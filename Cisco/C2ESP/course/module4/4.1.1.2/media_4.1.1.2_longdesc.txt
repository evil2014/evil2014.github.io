Figure is an animation that shows a router with a switch connected to one Ethernet port and another switch connected to a serial port.  Each of the switches has a computer connected to it.  The figure then shows a close-up view of the router, which is a Cisco model 1841.  The top comes off of the router and exposes the inside of the router, which is made up of a circuit board.  The output of the show i.p. route command is displayed and it shows two connected networks.