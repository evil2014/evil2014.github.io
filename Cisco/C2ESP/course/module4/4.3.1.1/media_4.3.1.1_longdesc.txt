Figure shows two routers connected together through a WAN serial link.  Each router has two local networks connected to LAN interfaces.  Any network that is assigned to a router’s local interface is considered a directly connected network and all networks that are connected to other routers are considered remote networks.