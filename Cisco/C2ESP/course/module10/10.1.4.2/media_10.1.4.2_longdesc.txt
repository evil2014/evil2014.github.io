Figure shows the output of the command, show running hyphen config pipe section interface gigabit Ethernet zero slash zero.  The output will list the configuration settings for the specified interface, including any d.h.c.p. settings.  In the figure, the output lists an i.p. address and an i.p. helper address.  The figure then shows the output of the command, show running hyphen config pipe include no service d.h.c.p.  In the example the output is blank.