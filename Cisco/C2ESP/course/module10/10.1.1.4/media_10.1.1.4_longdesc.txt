Figure one shows the d.h.c.p. version 4 discover message. The messages consists of the client’s MAC address as the source, a destination MAC address of all F’s, an i.p. address of zero dot zero dot zero dot zero is used for the source i.p., the subnet mask, and the default gateway.  The destination address is 255 dot 255 dot 255 dot 255.Figure two shows the d.h.c.p.v.4. offer message.  The server responds by offering an i.p. address and subnet mask.