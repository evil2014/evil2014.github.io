Figure shows a switch with two routers connected to it. One router is a stateless d.h.c.p.v.6. server and the other is a d.h.c.p.v.6. client.  The figure shows the commands used to configure the router as a d.h.c.p.v.6 client.  The figure begins in global configuration mode and the first command is, interface g zero slash one.  The next command is, i.p.v.6. enable.  The final command is, i.p.v.6. address autoconfig.