Figure shows a switch with a router and a computer connected to it.  The computer is sending out the i.p.v.6. router solicitation message, which is an all routers multicast message.  The router receives this message and responds with a router advertisement message, which is an i.p.v.6. all nodes multicast message.