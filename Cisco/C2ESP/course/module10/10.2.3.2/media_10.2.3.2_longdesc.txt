The figure shows the commands used to configure a router as a stateful d.h.c.p.v.6. client.  The example begins in global configuration mode and begins by moving to the desired interface.  The first command is, interface g zero slash 1.  The next command is, i.p.v.6. enable.  The last command is, i.p.v.6. address d.h.c.p.