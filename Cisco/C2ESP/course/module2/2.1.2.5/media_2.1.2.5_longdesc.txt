Figure one shows the output from the command, show interface fast Ethernet zero slash 1.  The operational status line is highlighted. It states, Fast Ethernet zero slash one is up, line protocol is up. Figure 2 shows the output from the, show interface command output. The counters and statistics for the Fast Ethernet zero slash one interface are highlighted.  The counters list statistics, such as zero runts, zero giants, 3 input errors, and 8 output errors. Figure three shows a table listing network access layer errors and their description.