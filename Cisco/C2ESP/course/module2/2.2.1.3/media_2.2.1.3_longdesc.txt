Figure one shows the configuration window for putty. In this example, the connection type is set to s.s.h., the port is set to 22, and the i.p. address of the host to connect to is set. Figure two shows the actual connection window for putty. In this example, the switch is asking for the username and the password. Figure three shows the output from the command, show i.p. s.s.h.  The output lists the version of s.s.h. running on the switch, as well as other configuration information.  Figure three also shows the output from the command, show s.s.h..  This output lists the current s.s.h. connections running on the switch.