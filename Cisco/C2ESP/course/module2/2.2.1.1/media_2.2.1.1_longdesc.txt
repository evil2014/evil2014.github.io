Figure one is a packet capture file of a telnet session using Wireshark. Figure two shows the details of the telnet session.  Here the data can be read since the data it is not encrypted. Figure three is a packet capture file of an s.s.h. session. Figure four shows the details of the s.s.h. session and the data is encrypted and cannot be read. Figure five shows the output from the command, show version, which displays the current i.o.s. running on the switch.  The letter K and the number 9 in the filename indicate that it supports cryptographic features.