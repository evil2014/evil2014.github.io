Figure one shows the O.S.P.F. reference topology. Figure two shows the output from the following command run on router R-1, show i.p. O.S.P.F. The figure lists two neighbor routers, routers R-2 and R-3.Figure three is an interactive media element that allows the learner to verify router R-1’s O.S.P.F. neighbors.