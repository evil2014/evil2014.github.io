Figure one shows the output from the following command run on router R-1, show i.p.v.6. O.S.P.F. neighbor.  Figure two is an interactive media element that allows the learner to verify the O.S.P.F.v.3 neighbors for router R-2.