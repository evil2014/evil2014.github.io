Figure one shows the output from the command, show i.p.v.6. route O.S.P.F.  The output lists three routes in the routing table for router R-1 that were learned via O.S.P.F. Figure two is an interactive media element that allows the learner to verify the i.p.v.6. routing table on routers R-2 and R-3.