Figure one is an animation which demonstrates that when a computer sends a broadcast packet, all other computers receive the packet when there is no v. lan segmentation. Figure two is an animation which demonstrates that when a computer sends a broadcast packet, only computers in the same v. lan will receive the packet when there is v. lan segmentation.