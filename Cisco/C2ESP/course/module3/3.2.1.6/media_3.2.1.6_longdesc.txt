Figure one shows a table with the options for the "show v. lan" command, and a table with the options for the "show interfaces" command and their descriptions. Figure two shows the output from the "show v. lan name student" command and the "show v. lan summary" command. Figure three shows the output from the "show interfaces v. lan twenty" command. Figure four is an interactive activity that allows the learner to practice using show commands for working with v. lans.