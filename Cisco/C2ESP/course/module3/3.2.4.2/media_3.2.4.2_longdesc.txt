Figure one is a flowchart showing the steps involved in diagnosing problems with connectivity when v. lans are in use. Figure two shows the output from the "show mac address dash table interface fast ethernet zero slash one" command on switch one and the output from the "show interfaces fast ethernet zero slash one switchport" on switch one.