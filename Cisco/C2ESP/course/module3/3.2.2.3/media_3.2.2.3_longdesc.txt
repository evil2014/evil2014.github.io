Figure one shows the syntax for configuring port f. zero slash one as a trunk port and setting the native v. lan to ninety nine.  The output from the "show interfaces f. zero slash one switchport" is also displayed to verify the changes have been applied correctly. Figure two is an interactive activity that allows the learner to practice configuring a port as a trunk port and setting the native v. lan.