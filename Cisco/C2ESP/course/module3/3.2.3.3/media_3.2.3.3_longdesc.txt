Figure is an interactive activity that shows four switches with connections between them.  The ports are labeled with their d. t. p. mode.  The learner must match each link with either trunk or access based on the port modes.