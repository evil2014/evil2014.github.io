Figure one shows a table with the commands for resetting the native v. lan and resetting the allowed v. lans on a trunk link. Figure two shows the syntax for resetting the native v. lan and resetting the allowed v. lans on a trunk link. The output from the "show interfaces f. zero slash one switchport" command to verify the port has been reconfigured properly is also shown. Figure three shows the syntax for configuring port f. zero slash one as an access port. The "show interfaces f. zero slash one switchport" command to verify the port has been reconfigured properly is also shown.